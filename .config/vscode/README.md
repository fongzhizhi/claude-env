# VSCode 配置说明

## 如何使用

### 方式一：一键部署（推荐）
```bash
npm run deploy:vscode
```

### 方式二：手动合并
1. 打开 VSCode，按 `Ctrl + Shift + P`
2. 输入 `Preferences: Open User Settings (JSON)`
3. 将 `settings.json` 中的内容合并进去

## 配置内容
- `claudeCode.*`：Claude Code 插件专属配置
- `git.*`：Git 相关配置
- `terminal.*`：终端相关配置
